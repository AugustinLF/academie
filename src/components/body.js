import g from "glamorous";

import {phone, tabletUp} from "./mediaQueries";

const Body = g.div({
    margin: "0 auto",
    paddingTop: 0,
    position: "relative",
    top: 0,
    left: 0,
    minHeight: "100%",
    margin: 0,
    width: "100%",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
});
export default Body;
