import g from "glamorous";

const Body = g.div({
    paddingTop: 0,
    position: "relative",
    top: 0,
    left: 0,
    minHeight: "100%",
    margin: 0,
    width: "100%",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
});
export default Body;
