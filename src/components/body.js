import g from "glamorous";

import {phone, tabletUp} from "./mediaQueries";

const Body = g.div({
    margin: "0 auto",
    padding: "0px 1.0875rem 1.45rem",
    paddingTop: 0,
    position: "relative",
    top: 0,
    left: 0,
    minHeight: "100%",
    margin: 0,
    width: "100%",
});
export default Body;
